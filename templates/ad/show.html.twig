{% extends "base.html.twig" %} {% block title %}
{{ ad.title }}
{% endblock title %} {% block body %}

<div
  class="ad-title"
  style="background-image: url({{ad.coverImage}}); background-repeat: no-repeat">
  <div class="container">
    <h1>{{ ad.title }}</h1>
    <p>{{ ad.introduction }}</p>
    <p class="h2">
      <strong>{{ ad.rooms }} chambres</strong> pour
      <strong>{{ ad.price }}</strong
      >&euro; par nuit
    </p>
    <a href="#" class="btn btn-primary">Réserver !</a>
  </div>
</div>

<div class="container">
  <div class="row">
    <div class="col-md-8 mb-3">
      <!-- le filtre raw affiche le texte sans protection et permet donc l'interpretation HTML -->
      {{ ad.content | raw }}

      <div
        id="carouselExampleIndicators"
        class="carousel slide"
        data-ride="carousel"
      >
        <ol class="carousel-indicators">
            {% for img in ad.images %}
          <li
            data-target="#carouselExampleIndicators"
            data-slide-to="{{ loop.index0 }}"
            class="{% if loop.first %} active {% endif %}"
          ></li>
          {% endfor %}
        </ol>
        <div class="carousel-inner">
          {% for img in ad.images %}
          <div class="carousel-item {% if loop.first %} active {% endif %}">
            <img src="{{img.url}}" class="d-block w-100" alt="..." />

            <div class="carousel-caption d-none d-md-block">
                <h5>{{ad.title}}</h5>
                <p>{{ img.caption }}</p>
              </div>
          </div>
          {% endfor %}
        </div>
        <a
          class="carousel-control-prev"
          href="#carouselExampleIndicators"
          role="button"
          data-slide="prev"
        >
          <span class="carousel-control-prev-icon" aria-hidden="true"></span>
          <span class="sr-only">Previous</span>
        </a>
        <a
          class="carousel-control-next"
          href="#carouselExampleIndicators"
          role="button"
          data-slide="next"
        >
          <span class="carousel-control-next-icon" aria-hidden="true"></span>
          <span class="sr-only">Next</span>
        </a>
      </div>
    </div>
    <div class="col">
      <div class="row">
        <div class="col-4">
          <img src="http://placehold.it/64x64" alt="avat de joseph" />
        </div>
        <div class="col">
          <h3>jospeh dupond</h3>
          <span class="badge badge-primary">3 annonces</span>
        </div>
      </div>
      <p>
        Lorem, ipsum dolor sit amet consectetur adipisicing elit. Mollitia velit
        itaque error, ratione quidem adipisci eum? Pariatur, vero? Ullam, nobis.
      </p>
      <p>
        Tempora aperiam quasi fugit ullam reprehenderit similique aliquid neque
        mollitia distinctio veniam, accusamus, temporibus, quis explicabo sit.
        Consequuntur, vel sit.
      </p>
      <p>
        Architecto animi necessitatibus at tenetur dignissimos aspernatur neque,
        nostrum corporis asperiores laudantium, iste omnis. Hic molestias
        adipisci vel dicta deleniti.
      </p>
    </div>
  </div>
</div>

{% endblock body %}
