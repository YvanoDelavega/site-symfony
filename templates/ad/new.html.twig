{% extends "base.html.twig" %} {% block title %}Création d'une annonce {%
endblock title %} {% block body %}

<div class="container">
  <h1>créer une annonce</h1>

  {#} {{ form(form) }} #}

  {{ form_start(form) }}
  {{ form_widget(form) }}
  <button type="submit" class="btn btn-primary">
    Créer la nouvelle annonce
  </button>
  {{ form_end(form) }}
</div>

{% endblock body %}
